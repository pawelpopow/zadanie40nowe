let paragraf=document.querySelector('p')
paragraf.textContent = 'name';
console.log(paragraf);

function tekst (value) {
    let plik = value;
    var button = document.getElementById('Wyświetl dane');
    console.log(button.value);  {
        return value;
    }

}